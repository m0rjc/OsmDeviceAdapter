# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.

# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/

# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~

# Various IDEs
.project
.idea/
*.tmproj
.vscode/

# Values files with actual secrets (NEVER package these!)
values-production.yaml
values-prod.yaml
values-*.yaml
!values-example.yaml

# OS files
.DS_Store
Thumbs.db

# Documentation that doesn't need to be in the package
README.md
